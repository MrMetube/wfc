\begin{figure}[H]
    \includegraphics[width=\linewidth]{data/ms_algorithm/1.png}
    
    \caption{
         Visualisierung des Ablaufs des Model Synthesis Algorithmus \cite{merrel}.
         Die Ausgabe wird mit dem leeren Zustand initialisiert. Dann werden Ausschnitte(rot) geleert und jeder Zelle werden alle Zustände zugewiesen. Die Region wird gelöst und die nächste Region überschneidet sich mit der ersten.
    }
    \label{fig:ms_algorithm}
\end{figure}